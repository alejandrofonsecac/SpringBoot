version: '3.1'
services:
  db:
    image: mysql:8
    container_name: mysql-spring
    ports:
      - "3308:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - data:/var/lib/mysql-spring

volumes:
  data: